# 环境配置指南

> 物理AI开发环境搭建完整指南

## 📋 概述

本文档详细介绍物理AI/具身智能开发的必备环境配置，包括基础软件、深度学习框架、机器人仿真环境等。

---

## 🖥️ 硬件要求

### 最低配置（仅仿真）
| 组件 | 要求 |
|------|------|
| CPU | Intel i5 / AMD Ryzen 5 及以上 |
| 内存 | 16GB RAM |
| 存储 | 50GB SSD |
| GPU | NVIDIA GTX 1060 6GB 及以上 |

### 推荐配置（深度学习+仿真）
| 组件 | 要求 |
|------|------|
| CPU | Intel i7 / AMD Ryzen 7 及以上 |
| 内存 | 32GB RAM |
| 存储 | 100GB NVMe SSD |
| GPU | NVIDIA RTX 3070 8GB 及以上 |

---

## 🐍 1. Python环境配置

### 使用Anaconda（推荐）

```bash
# 创建虚拟环境
conda create -n physical-ai python=3.10 -y
conda activate physical-ai

# 升级pip
pip install --upgrade pip setuptools wheel
```

### 使用venv

```bash
# 创建虚拟环境
python -m venv physical-ai
source physical-ai/bin/activate  # Linux/Mac
# 或
physical-ai\Scripts\activate  # Windows

# 升级pip
pip install --upgrade pip
```

---

## 🔥 2. PyTorch 安装

### GPU版本（推荐）

```bash
# CUDA 11.8
pip install torch==2.1.0 torchvision==0.16.0 --index-url https://download.pytorch.org/whl/cu118

# CUDA 12.1（推荐）
pip install torch==2.2.0 torchvision==0.17.0 --index-url https://download.pytorch.org/whl/cu121

# 验证安装
python -c "import torch; print(f'PyTorch: {torch.__version__}'); print(f'CUDA: {torch.cuda.is_available()}')"
```

### CPU版本

```bash
pip install torch==2.2.0 torchvision==0.17.0 --index-url https://download.pytorch.org/whl/cpu
```

---

## 🤖 3. 机器人相关库

### 基础依赖

```bash
# 数值计算
pip install numpy scipy matplotlib

# 机器学习
pip install scikit-learn pandas

# 进度条和日志
pip install tqdm loguru
```

### OpenCV

```bash
# CPU版本
pip install opencv-python

# 带CUDA支持（可选）
pip install opencv-python
# 编译时需启用CUDA支持
```

### 机械臂控制库

```bash
# PyBullet仿真
pip install pybullet

# ROS2客户端（可选）
pip install rclpy
```

---

## 🎮 4. LeRobot 安装

HuggingFace LeRobot 是目前最流行的开源机器人学习框架。

```bash
# 从PyPI安装（推荐）
pip install lerobot

# 或从源码安装
git clone https://github.com/huggingface/lerobot.git
cd lerobot
pip install -e .

# 验证安装
lerobot-info
```

### LeRobot 依赖

```bash
# 额外依赖
pip install "lerobot[dev]"
pip install av moviepy
```

---

## 🏭 5. Isaac Sim 安装（可选）

NVIDIA Isaac Sim 是强大的机器人仿真平台。

### 系统要求

| 组件 | 要求 |
|------|------|
| OS | Ubuntu 20.04/22.04 或 Windows 10/11 |
| GPU | NVIDIA RTX 3060 及以上 |
| 显存 | 8GB+ |
| 驱动 | NVIDIA Driver 525+ |

### 安装步骤

#### 方式一：Omniverse Launcher（推荐）

1. 下载 [Omniverse Launcher](https://www.nvidia.com/en-us/omniverse/download/)
2. 安装后登录NVIDIA账号
3. 在"Exchange"中搜索"Isaac Sim"
4. 点击安装

#### 方式二：Docker（Linux）

```bash
# 1. 安装NVIDIA Container Toolkit
distribution=$(. /etc/os-release;echo $ID$VERSION_ID)
curl -s -L https://nvidia.github.io/nvidia-docker/gpgkey | sudo apt-key add -
curl -s -L https://nvidia.github.io/nvidia-docker/$distribution/nvidia-docker.list | \
    sudo tee /etc/apt/sources.list.d/nvidia-docker.list
sudo apt-get update && sudo apt-get install -y nvidia-container-toolkit

# 2. 配置Docker
sudo nvidia-ctk runtime configure --runtime=docker
sudo systemctl restart docker

# 3. 拉取Isaac Sim镜像
docker pull nvcr.io/nvidia/isaac-sim:latest

# 4. 运行容器
docker run --name isaac-sim --entrypoint bash -it --gpus all \
    -e "ACCEPT_EULA=Y" -e "PRIVACY_CONSENT=Y" \
    --rm --network=host \
    -v ~/isaac-sim/cache:/isaac-sim/kit/cache:rw \
    nvcr.io/nvidia/isaac-sim:latest \
    ./runheadless.native.sh -v
```

---

## 🐢 6. MuJoCo 安装

```bash
# 安装MuJoCo
pip install mujoco

# 验证安装
python -c "import mujoco; print(mujoco.__version__)"
```

### mujoco-py（可选，用于Python绑定）

```bash
# 下载MuJoCo二进制文件
mkdir -p ~/.mujoco
wget -q https://mujoco.org/bin/mujoco210_linux.zip -O /tmp/mujoco.zip
unzip /tmp/mujoco.zip -d ~/.mujoco/
mv ~/.mujoco/mujoco210_linux ~/.mujoco/mujoco210

# 设置环境变量
export MUJOCO_KEY_PATH=~/.mujoco/mujoco210/bin:/key.bin
export LD_LIBRARY_PATH=$LD_LIBRARY_PATH:$MUJOCO_KEY_PATH

# 安装Python绑定
pip install mujoco-py
```

---

## 📦 7. ROS 2 安装（可选）

ROS 2 是机器人开发的标准中间件。

### Ubuntu 22.04

```bash
# 设置locale
sudo locale-gen en_US en_US.UTF-8
sudo update-locale LC_ALL=en_US.UTF-8 LANG=en_US.UTF-8

# 设置源
sudo apt update && sudo apt install -y software-properties-common
sudo add-apt-repository universe

# 添加ROS 2 GPG密钥
sudo curl -sSL https://raw.githubusercontent.com/ros/rosdistro/master/ros.key | sudo gpg --dearmor -o /usr/share/keyrings/ros-archive-keyring.gpg

# 添加源
echo "deb [arch=$(dpkg --print-architecture) signed-by=/usr/share/keyrings/ros-archive-keyring.gpg] http://packages.ros.org/ros2/ubuntu $(lsb_release -cs) main" | sudo tee /etc/apt/sources.list.d/ros2.list > /dev/null

# 安装ROS 2
sudo apt update
sudo apt install -y ros-humble-desktop

# 安装Python包
sudo apt install -y python3-colcon-common-extensions python3-rosdep python3-vcstool

# 初始化
source /opt/ros/humble/setup.bash

# 安装常用包
sudo apt install -y ros-humble-moveit-msgs ros-humble-moveit
```

---

## 🧪 8. 环境验证

创建一个验证脚本：

```python
# env_check.py
import sys

def check_environment():
    results = []
    
    # Python
    results.append(("Python", f"{sys.version.split()[0]}", True))
    
    # PyTorch
    try:
        import torch
        results.append(("PyTorch", torch.__version__, True))
        results.append(("CUDA Available", str(torch.cuda.is_available()), torch.cuda.is_available()))
    except ImportError as e:
        results.append(("PyTorch", "Not installed", False))
    
    # NumPy
    try:
        import numpy as np
        results.append(("NumPy", np.__version__, True))
    except ImportError:
        results.append(("NumPy", "Not installed", False))
    
    # OpenCV
    try:
        import cv2
        results.append(("OpenCV", cv2.__version__, True))
    except ImportError:
        results.append(("OpenCV", "Not installed", False))
    
    # PyBullet
    try:
        import pybullet
        import pybullet_data
        results.append(("PyBullet", pybullet.__version__, True))
    except ImportError:
        results.append(("PyBullet", "Not installed", False))
    
    # LeRobot
    try:
        import lerobot
        results.append(("LeRobot", lerobot.__version__, True))
    except ImportError:
        results.append(("LeRobot", "Not installed", False))
    
    # 打印结果
    print("=" * 50)
    print("Environment Check Results")
    print("=" * 50)
    for name, version, status in results:
        status_str = "✅" if status else "❌"
        print(f"{status_str} {name:15s} : {version}")
    print("=" * 50)
    
    all_ok = all(r[2] for r in results)
    return all_ok

if __name__ == "__main__":
    success = check_environment()
    sys.exit(0 if success else 1)
```

运行验证：

```bash
python env_check.py
```

---

## 🔧 常见问题

### Q1: CUDA不可用
**解决**：
1. 确认安装了NVIDIA驱动：`nvidia-smi`
2. 确认PyTorch版本与CUDA版本匹配
3. 重新安装正确版本的PyTorch

### Q2: ImportError: libGL.so.1
**解决**：
```bash
sudo apt install libgl1-mesa-glx libgl1-mesa-dev
```

### Q3: MuJoCo license问题
**解决**：
```bash
# 免费许可证
touch ~/.mujoco/mujoco200_linux.bin
# 或使用mujoco-py时在代码中设置
```

---

## 📚 参考资源

- [PyTorch Installation](https://pytorch.org/get-started/locally/)
- [LeRobot Documentation](https://huggingface.co/docs/lerobot)
- [Isaac Sim Documentation](https://docs.omniverse.nvidia.com/isaacsim)
- [ROS 2 Documentation](https://docs.ros.org/en/humble/)

---

*下节教程：[LeRobot入门教程](./LeRobot入门教程.md)*
